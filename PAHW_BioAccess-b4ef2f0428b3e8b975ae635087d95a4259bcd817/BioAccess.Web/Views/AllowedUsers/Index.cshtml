@model List<Terminals.Web.DTOs.AllowedUserListItemDto>
@{
    ViewData["Title"] = "Ø¥Ø¯Ø§Ø±Ø© Ø§Ù„Ù…Ø³ØªØ®Ø¯Ù…ÙŠÙ†";
    var currentEmpId = Context.Session.GetString("EmpId") ?? "";
}

<div class="content-card fade-in">

    @if (TempData["ErrorMsg"] != null)
    {
        <div class="alert alert-error">
            <span class="alert-icon">âš ï¸</span>
            <span>@TempData["ErrorMsg"]</span>
        </div>
    }
    @if (TempData["SuccessMsg"] != null)
    {
        <div class="alert alert-success">
            <span class="alert-icon">âœ…</span>
            <span>@TempData["SuccessMsg"]</span>
        </div>
    }

    <div class="page-header">
        <h1 class="page-title">
            <span class="page-title-icon">ðŸ‘¥</span>
            <span>Ø¥Ø¯Ø§Ø±Ø© Ø§Ù„Ù…Ø³ØªØ®Ø¯Ù…ÙŠÙ†</span>
        </h1>
        <a asp-action="Create" class="btn btn-primary">
            <span>âž•</span>
            <span>Ø¥Ø¶Ø§ÙØ© Ù…Ø³ØªØ®Ø¯Ù…</span>
        </a>
    </div>

    <div class="divider"></div>

    <div class="c-stats-grid">
        <div class="stat-card">
            <div class="stat-value">@Model.Count</div>
            <div class="stat-label">Ø¥Ø¬Ù…Ø§Ù„ÙŠ Ø§Ù„Ù…Ø³ØªØ®Ø¯Ù…ÙŠÙ†</div>
        </div>
        <div class="stat-card">
            <div class="stat-value text-emerald">@Model.Count(u => u.IsActive)</div>
            <div class="stat-label">Ø§Ù„Ø­Ø³Ø§Ø¨Ø§Øª Ø§Ù„Ù†Ø´Ø·Ø©</div>
        </div>
        <div class="stat-card">
            <div class="stat-value text-rose">@Model.Count(u => !u.IsActive)</div>
            <div class="stat-label">Ø§Ù„Ø­Ø³Ø§Ø¨Ø§Øª Ø§Ù„Ù…Ø¹Ø·Ù‘Ù„Ø©</div>
        </div>
        <div class="stat-card">
            <div class="stat-value text-blue">@Model.Count(u => u.IsAdmin)</div>
            <div class="stat-label">Ø§Ù„Ù…Ø¯Ø±Ø§Ø¡</div>
        </div>
    </div>

    @if (Model.Any())
    {
        <div class="table-container">
            <table class="data-table">
                <thead>
                    <tr>
                        <th>Ø§Ø³Ù… Ø§Ù„Ù…Ø³ØªØ®Ø¯Ù…</th>
                        <th>Ø§Ù„Ø§Ø³Ù… Ø§Ù„ÙƒØ§Ù…Ù„</th>
                        <th>Ø§Ù„Ø¨Ø±ÙŠØ¯ Ø§Ù„Ø¥Ù„ÙƒØªØ±ÙˆÙ†ÙŠ</th>
                        <th>Ø§Ù„Ù‚Ø³Ù…</th>
                        <th>ØµÙ„Ø§Ø­ÙŠØ© Ù…Ø¯ÙŠØ±</th>
                        <th>Ø§Ù„Ø­Ø§Ù„Ø©</th>
                        <th>ØµØ§Ù„Ø­ Ø­ØªÙ‰</th>
                        <th class="u-text-center">Ø§Ù„Ø¥Ø¬Ø±Ø§Ø¡Ø§Øª</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var u in Model)
                    {
                        <tr>
                            <td><strong class="text-maroon">@u.EmployeeId</strong></td>
                            <td><strong>@u.FullName</strong></td>
                            <td><span class="text-muted">@u.Email</span></td>
                            <td>@u.Department</td>
                            <td>
                                @if (u.IsAdmin)
                                {
                                    <span class="badge badge-info"><span>â­</span> <span>Ù…Ø¯ÙŠØ±</span></span>
                                }
                                else
                                {
                                    <span class="badge badge-muted"><span>ðŸ‘¤</span> <span>Ù…Ø³ØªØ®Ø¯Ù…</span></span>
                                }
                            </td>
                            <td>
                                @if (u.IsActive)
                                {
                                    <span class="badge badge-success"><span>âœ“</span> <span>ÙØ¹Ø§Ù„</span></span>
                                }
                                else
                                {
                                    <span class="badge badge-danger"><span>âœ•</span> <span>Ù…Ø¹Ø·Ù„</span></span>
                                }
                            </td>
                            <td>
                                @if (u.ValidUntil.HasValue)
                                {
                                    <span class="text-amber u-fw-700">@u.ValidUntil.Value.ToString("yyyy-MM-dd")</span>
                                }
                                else
                                {
                                    <span class="text-gray-400">Ø¯Ø§Ø¦Ù…</span>
                                }
                            </td>
                            <td>
                                <div class="c-actions-row">
                                    @if (u.EmployeeId.ToString() != currentEmpId)
                                    {
                                        <form method="post" asp-action="Toggle" class="u-inline">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="employeeId" value="@u.EmployeeId" />
                                            <input type="hidden" name="makeActive" value="@(u.IsActive ? "false" : "true")" />
                                            <button type="submit" class="btn btn-sm @(u.IsActive ? "btn-secondary" : "btn-success")">
                                                @if (u.IsActive) { <span>â¸ï¸ ØªØ¹Ø·ÙŠÙ„</span> } else { <span>â–¶ï¸ ØªÙØ¹ÙŠÙ„</span> }
                                            </button>
                                        </form>
                                        <form method="post" asp-action="Delete" class="u-inline"
                                              onsubmit="return confirm('âš ï¸ Ù‡Ù„ Ø£Ù†Øª Ù…ØªØ£ÙƒØ¯ Ù…Ù† Ø§Ù„Ø­Ø°Ù Ø§Ù„Ù†Ù‡Ø§Ø¦ÙŠ Ù„Ù‡Ø°Ø§ Ø§Ù„Ù…Ø³ØªØ®Ø¯Ù…ØŸ');">
                                            @Html.AntiForgeryToken()
                                            <input type="hidden" name="employeeId" value="@u.EmployeeId" />
                                            <button type="submit" class="btn btn-sm btn-danger">ðŸ—‘ï¸ Ø­Ø°Ù</button>
                                        </form>
                                    }
                                    else
                                    {
                                        <span class="badge badge-copper">ðŸ”’ Ø­Ø³Ø§Ø¨Ùƒ</span>
                                    }
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    }
    else
    {
        <div class="empty-state">
            <div class="empty-state-icon">ðŸ“­</div>
            <div class="empty-state-text">Ù„Ø§ ÙŠÙˆØ¬Ø¯ Ù…Ø³ØªØ®Ø¯Ù…ÙˆÙ† ÙÙŠ Ø§Ù„Ù‚Ø§Ø¦Ù…Ø© Ø­Ø§Ù„ÙŠØ§Ù‹</div>
            <div class="u-mt-24">
                <a asp-action="Create" class="btn btn-primary"><span>âž•</span> <span>Ø¥Ø¶Ø§ÙØ© Ù…Ø³ØªØ®Ø¯Ù…</span></a>
            </div>
        </div>
    }

</div>
